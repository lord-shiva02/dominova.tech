{
  "buildCommand": "cd \"dominova tech/client\" && npm install && npm run build",
  "outputDirectory": "dominova tech/client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
